package com.example.jee.config;

/**
 * Channel-name constants — exercises constant-based channel resolution
 * (Class.CONST and bare CONST references in producers).
 */
public final class Channels {
    public static final String ORDER_EVENTS = "order-events";
    public static final String INVENTORY_UPDATES = "inventory-updates";
    public static final String NOTIFICATIONS = "notifications";
}
